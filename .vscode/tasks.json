{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run (Windows)",
            "type": "shell",

            "windows": {
                "command": "run.cmd"
            },

            "linux": {
                "command": "bash run.sh"
            },

            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}